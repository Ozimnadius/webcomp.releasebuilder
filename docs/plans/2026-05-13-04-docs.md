# ReleaseBuilder: Documentation Update

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Удалить `include/vendor.php`, дополнить PHPDoc во всех PHP-файлах модуля и создать актуальный `README.md`.

**Architecture:** Три независимые задачи: (1) удаление устаревшего файла и PHPDoc для инсталлятора и контроллера, (2) верификация уже существующего PHPDoc в `lib/`, (3) написание README. Изменений в логике нет — только документация.

**Tech Stack:** PHP 8.2, PHPDoc, Markdown.

---

## Файлы

| Действие | Путь |
|---|---|
| Удалить | `include/vendor.php` |
| Изменить | `install/index.php` |
| Изменить | `lib/controller/releasebuilder.php` |
| Проверить/изменить | `lib/DTO/ScanRequest.php` |
| Проверить/изменить | `lib/Infrastructure/LocalFilesystem.php` |
| Создать | `README.md` |

---

### Task 15: Удалить vendor.php, добавить PHPDoc в install/index.php и controller

**Files:**
- Delete: `include/vendor.php`
- Modify: `install/index.php`
- Modify: `lib/controller/releasebuilder.php`

- [ ] **Step 1: Удалить `include/vendor.php`**

```bash
git rm include/vendor.php
```

Ожидаемый вывод: `rm 'include/vendor.php'`

- [ ] **Step 2: Добавить класс-блок в `install/index.php`**

Открыть `install/index.php`. Перед строкой `class webcomp_releasebuilder extends CModule` вставить:

```php
/**
 * Инсталлятор модуля webcomp.releasebuilder.
 *
 * Регистрирует модуль в Bitrix, копирует файлы интерфейса в публичные директории
 * и заполняет поля вендорной конфигурации для Bitrix Marketplace.
 */
```

- [ ] **Step 3: Добавить PHPDoc к методам `install/index.php`**

Перед `public function __construct()` вставить:

```php
/**
 * Инициализирует поля модуля из языкового файла и `install/version.php`.
 */
```

Перед `public function DoInstall(): void` вставить:

```php
/**
 * Устанавливает модуль: копирует файлы и регистрирует его в системе Bitrix.
 */
```

Перед `public function DoUninstall(): void` вставить:

```php
/**
 * Деинсталлирует модуль: удаляет файлы из публичных директорий и снимает регистрацию.
 */
```

Перед `public function InstallFiles(): void` вставить:

```php
/**
 * Копирует admin-страницы и JS/CSS-ресурсы из install/ в публичные директории Bitrix.
 */
```

Перед `public function UnInstallFiles(): void` вставить:

```php
/**
 * Удаляет admin-страницы и JS/CSS-ресурсы из публичных директорий Bitrix.
 */
```

Итоговый файл `install/index.php` целиком:

```php
<?php

use Bitrix\Main\Localization\Loc;

Loc::loadMessages(__FILE__);

/**
 * Инсталлятор модуля webcomp.releasebuilder.
 *
 * Регистрирует модуль в Bitrix, копирует файлы интерфейса в публичные директории
 * и заполняет поля вендорной конфигурации для Bitrix Marketplace.
 */
class webcomp_releasebuilder extends CModule
{
    public $MODULE_ID = 'webcomp.releasebuilder';
    public $MODULE_VERSION;
    public $MODULE_VERSION_DATE;
    public $MODULE_NAME;
    public $MODULE_DESCRIPTION;
    public $PARTNER_NAME;
    public $PARTNER_URI;

    const partnerName  = 'webcomp';
    const solutionName = 'releasebuilder';

    /**
     * Инициализирует поля модуля из языкового файла и `install/version.php`.
     */
    public function __construct()
    {
        $this->MODULE_NAME        = Loc::getMessage('WEBCOMP_RELEASEBUILDER_MODULE_NAME');
        $this->MODULE_DESCRIPTION = Loc::getMessage('WEBCOMP_RELEASEBUILDER_MODULE_DESC');
        $this->PARTNER_NAME       = Loc::getMessage('WEBCOMP_RELEASEBUILDER_PARTNER_NAME');
        $this->PARTNER_URI        = Loc::getMessage('WEBCOMP_RELEASEBUILDER_PARTNER_URI');

        $this->SHOW_SUPER_ADMIN_GROUP_RIGHTS = 'Y';
        $this->MODULE_GROUP_RIGHTS           = 'Y';

        include __DIR__ . '/version.php';

        if (isset($arModuleVersion['VERSION'])) {
            $this->MODULE_VERSION      = $arModuleVersion['VERSION'];
            $this->MODULE_VERSION_DATE = $arModuleVersion['VERSION_DATE'] ?? null;
        }
    }

    /**
     * Устанавливает модуль: копирует файлы и регистрирует его в системе Bitrix.
     */
    public function DoInstall(): void
    {
        $this->InstallFiles();
        RegisterModule($this->MODULE_ID);
    }

    /**
     * Деинсталлирует модуль: удаляет файлы из публичных директорий и снимает регистрацию.
     */
    public function DoUninstall(): void
    {
        $this->UnInstallFiles();
        UnRegisterModule($this->MODULE_ID);
    }

    /**
     * Копирует admin-страницы и JS/CSS-ресурсы из install/ в публичные директории Bitrix.
     */
    public function InstallFiles(): void
    {
        CopyDirFiles(__DIR__ . '/admin', $_SERVER['DOCUMENT_ROOT'] . '/bitrix/admin', true);
        CopyDirFiles(__DIR__ . '/js',  $_SERVER['DOCUMENT_ROOT'] . '/bitrix/js/'  . $this->MODULE_ID, true);
        CopyDirFiles(__DIR__ . '/css', $_SERVER['DOCUMENT_ROOT'] . '/bitrix/css/' . $this->MODULE_ID, true);
    }

    /**
     * Удаляет admin-страницы и JS/CSS-ресурсы из публичных директорий Bitrix.
     */
    public function UnInstallFiles(): void
    {
        DeleteDirFiles(__DIR__ . '/admin', $_SERVER['DOCUMENT_ROOT'] . '/bitrix/admin');
        DeleteDirFiles(__DIR__ . '/js',  $_SERVER['DOCUMENT_ROOT'] . '/bitrix/js/'  . $this->MODULE_ID);
        DeleteDirFiles(__DIR__ . '/css', $_SERVER['DOCUMENT_ROOT'] . '/bitrix/css/' . $this->MODULE_ID);
    }
}
```

- [ ] **Step 4: Заменить PHPDoc у action-методов в контроллере**

Открыть `lib/controller/releasebuilder.php`.

Заменить существующий однострочный комментарий над `versionAction` на:

```php
/**
 * Возвращает VERSION и VERSION_DATE для указанного модуля.
 *
 * @param string $module Идентификатор модуля (например, 'webcomp.market').
 * @return array{VERSION: string, VERSION_DATE: string}|null Данные версии или null при ошибке.
 */
```

Заменить над `getConfigAction`:

```php
/**
 * Возвращает конфиг модуля: паттерны, имя шаблона, доступные шаблоны, тип модуля.
 *
 * @param string $module Идентификатор модуля.
 * @return array{patterns: string[], templateName: string, availableTemplates: string[], suggestedTemplate: string, moduleType: string}|null Конфиг или null при ошибке.
 */
```

Заменить над `saveConfigAction`:

```php
/**
 * Сохраняет паттерны и имя шаблона для модуля.
 *
 * @param string $module       Идентификатор модуля.
 * @param string $patterns     Паттерны исключений, по одному на строку (разделитель \n).
 * @param string $templateName Название шаблона; допустимые символы: [a-zA-Z0-9_\-.]. Пустая строка допустима.
 * @return bool|null true при успехе, null при ошибке валидации.
 */
```

Заменить над `searchAction`:

```php
/**
 * Сканирует файлы модуля и возвращает список изменённых путей.
 *
 * @param string $module       Идентификатор модуля.
 * @param string $newVersion   Целевая версия в формате major.minor.patch.
 * @param string $version      Текущая установленная версия.
 * @param string $type         Тип модуля: 'regular' или 'template'.
 * @param string $date         Дата отсечки; файлы, изменённые позже, попадут в результат.
 * @param string $templateName Название шаблона (только для type='template').
 * @return string[]|null Список абсолютных путей изменённых файлов или null при ошибке.
 */
```

Заменить над `prepareArchiveAction`:

```php
/**
 * Копирует выбранные файлы, создаёт архив, удаляет staging-директорию.
 *
 * @param string $module        Идентификатор модуля.
 * @param string $newVersion    Целевая версия в формате major.minor.patch.
 * @param string $version       Текущая установленная версия.
 * @param string $type          Тип модуля: 'regular' или 'template'.
 * @param string $date          Дата отсечки для версии файлов.
 * @param string $description   Текст описания обновления (кодируется в Windows-1251).
 * @param string $templateName  Название шаблона (только для type='template').
 * @param string $selectedFiles JSON-массив абсолютных путей к выбранным файлам.
 * @return string|null Путь к архиву относительно docRoot или null при ошибке.
 */
```

- [ ] **Step 5: Добавить PHPDoc к приватным методам контроллера**

Перед `private function moduleService(): ModuleService` добавить:

```php
/**
 * Возвращает (или создаёт) единственный экземпляр ModuleService для текущего запроса.
 *
 * @return ModuleService
 */
```

Перед `private function filterConfig(): FilterConfig` добавить:

```php
/**
 * Возвращает (или создаёт) единственный экземпляр FilterConfig для текущего запроса.
 *
 * @return FilterConfig
 */
```

- [ ] **Step 6: Убедиться, что тесты остались зелёными**

```bash
vendor/bin/phpunit --testdox
```

Ожидаемый вывод: `OK (48 tests, 123 assertions)`

- [ ] **Step 7: Commit**

```bash
git add install/index.php lib/controller/releasebuilder.php
git commit -m "docs: add PHPDoc to installer and controller, remove vendor.php"
```

---

### Task 16: Верификация PHPDoc в lib/

**Files:**
- Verify/Modify: `lib/DTO/ScanRequest.php`
- Verify/Modify: `lib/Infrastructure/LocalFilesystem.php`
- Verify (no changes expected): все остальные файлы `lib/`

- [ ] **Step 1: Проверить `ScanRequest::fromPost()` — мёртвый код**

`fromPost()` нигде в модуле не вызывается. Проверить:

```bash
grep -rn "fromPost" lib/ install/ admin/ tests/ --include="*.php"
```

Ожидаемый вывод: только строка в `lib/DTO/ScanRequest.php` (само определение).

Добавить тег `@internal` к методу `fromPost()`:

```php
/**
 * Создаёт ScanRequest из сырого массива $_POST.
 *
 * @internal Метод не используется в текущей реализации контроллера. Контроллер
 *           выполняет валидацию и создаёт ScanRequest вручную.
 * @param array $post Данные POST-запроса. Ожидаемые ключи: module, type, version, newVersion, date.
 * @return self
 * @throws \InvalidArgumentException Если значение 'date' не удаётся преобразовать в Unix-timestamp.
 */
public static function fromPost(array $post): self
```

- [ ] **Step 2: Проверить `LocalFilesystem::deleteDir()` — отсутствие @throws**

Открыть `lib/Infrastructure/LocalFilesystem.php`. Метод `deleteDir()` помечен `/** {@inheritDoc} */`.
Интерфейс `FilesystemInterface::deleteDir()` декларирует `@throws \RuntimeException`.
Реализация при ошибке `unlink()` молча продолжает (не бросает). Это расхождение с контрактом.

Заменить `/** {@inheritDoc} */` перед `deleteDir()` на расширенный блок:

```php
/**
 * {@inheritDoc}
 *
 * Реализация: ошибки отдельных `unlink()` внутри директории игнорируются;
 * исключение не бросается. Это допустимо для cleanup-операций staging-директории.
 */
```

- [ ] **Step 3: Прочитать и сверить остальные 11 файлов lib/**

Прочитать каждый файл и убедиться, что `@param`/`@return`/`@throws` соответствуют сигнатурам:

- `lib/Contract/FilesystemInterface.php` — 9 методов
- `lib/Contract/ArchiveInterface.php` — 3 метода
- `lib/DTO/ArchiveRequest.php` — конструктор
- `lib/Enum/ModuleType.php` — 2 case
- `lib/Service/ScanFilter.php` — 1 публичный метод
- `lib/Service/PathMapper.php` — 2 публичных метода
- `lib/Service/ModuleService.php` — 5 публичных + 1 приватный
- `lib/Service/FileScanner.php` — 1 публичный + 2 приватных
- `lib/Service/FileCopier.php` — 2 публичных
- `lib/Service/FilterConfig.php` — 4 публичных + 1 приватный
- `lib/Service/ArchiveBuilder.php` — 1 публичный + 2 приватных
- `lib/Infrastructure/ZipArchiveAdapter.php` — 3 метода

Для каждого файла: если расхождений нет — переходим дальше. Если есть — правим на месте.

- [ ] **Step 4: Убедиться что тесты зелёные**

```bash
vendor/bin/phpunit --testdox
```

Ожидаемый вывод: `OK (48 tests, 123 assertions)`

- [ ] **Step 5: Commit**

Если были правки:
```bash
git add lib/
git commit -m "docs: verify and fix PHPDoc across lib/"
```

Если правок нет:
```bash
git commit --allow-empty -m "docs: lib/ PHPDoc verified, no changes needed"
```

---

### Task 17: README.md

**Files:**
- Create: `README.md`

- [ ] **Step 1: Создать `README.md` в корне модуля**

Создать файл `README.md` со следующим содержимым:

```markdown
# webcomp.releasebuilder

Bitrix-модуль для сборки обновлений вендорных модулей. Сканирует изменённые файлы,
копирует их в staging-папку, упаковывает в `.zip` с файлами `updater.php`, `version.php`
и `description.ru`.

---

## Быстрый старт

1. Перейти в Bitrix-админку → раздел **Release Builder**
2. Выбрать модуль из списка установленных вендорных модулей
3. Выбрать тип: **Обычный модуль** или **Шаблонное решение**
4. Указать дату последнего обновления — файлы, изменённые позже этой даты, попадут в архив
5. Нажать **Найти файлы** — появится список найденных изменений
6. Отметить нужные файлы, ввести описание обновления
7. Нажать **Создать архив** → скачать `.zip`

---

## Настройка фильтров

В поле «Фильтры исключений» указывается один паттерн на строку.
Поддерживаются три формата:

| Формат | Пример | Описание |
|--------|--------|----------|
| Точное имя | `node_modules` | Совпадение по имени файла или папки |
| Glob | `*.log`, `cache_*` | Шаблон с `*` и `?` |
| Regex | `/\.generated\.php$/` или `~^tmp_~` | Регулярное выражение (начинается с `/` или `~`) |

Паттерны сохраняются per-module в `upload/release-builder/config.json`.
Нажмите **Сохранить фильтры** — они применятся при следующем поиске.

> **Примечание:** папки, начинающиеся с точки (`.git`, `.idea`, `.osp`),
> исключаются автоматически на любом уровне вложенности.

---

## Шаблонные решения

При типе **Шаблонное решение** появляется поле **Название шаблона**
(например: `webcomp_yellow`). Значение сохраняется в `config.json`.

При сборке архива `updater.php` генерируется из шаблона
`bitrix/modules/webcomp.releasebuilder/install/updater.php.tpl`
с подстановкой плейсхолдеров:

| Плейсхолдер | Пример значения |
|-------------|-----------------|
| `{{MODULE}}` | `webcomp.market` |
| `{{VENDOR}}` | `webcomp` |
| `{{NAME}}` | `market` |
| `{{TEMPLATE_NAME}}` | `webcomp_yellow` |

Для обычного модуля поле оставьте пустым — строки шаблона с путями к визарду
не выполнятся (директории не существуют, `is_dir` вернёт `false`).

---

## Архитектура

```
HTTP (Bitrix AJAX-контроллер)
    │
    └── Controller\ReleaseBuilder   — валидация входных данных, оркестрация сервисов
            │
            ├── ModuleService    — список модулей, версии из version.php
            ├── FilterConfig     — чтение/запись config.json (паттерны + templateName)
            │
            ├── FileScanner      — рекурсивное сканирование изменённых файлов
            │       ├── ScanFilter   — логика «пропустить ли запись»
            │       └── PathMapper   — маппинг source → destination путей
            │
            ├── FileCopier       — копирование файлов в staging-папку версии
            │
            └── ArchiveBuilder   — сборка .zip архива
                    └── ZipArchiveAdapter — обёртка над PHP ZipArchive
```

Все сервисы получают зависимости через конструктор. Файловая система скрыта за
`FilesystemInterface`, что позволяет тестировать сервисы без реального диска.

---

## Структура файлов

```
bitrix/modules/webcomp.releasebuilder/
├── install/
│   ├── index.php          # Инсталлятор модуля (CModule)
│   ├── version.php        # Текущая версия модуля
│   └── updater.php.tpl    # Шаблон updater.php для архива
├── lang/ru/install/
│   └── index.php          # Локализация инсталлятора
├── lib/
│   ├── Contract/          # Интерфейсы (FilesystemInterface, ArchiveInterface)
│   ├── DTO/               # Объекты данных (ScanRequest, ArchiveRequest)
│   ├── Enum/              # ModuleType (regular / template)
│   ├── Service/           # Бизнес-логика (6 сервисов)
│   ├── Infrastructure/    # Реализации контрактов (LocalFilesystem, ZipArchiveAdapter)
│   └── controller/        # Bitrix AJAX-контроллер
├── tests/
│   ├── Service/           # Юнит-тесты (7 файлов, 48 тестов)
│   └── bootstrap.php
├── docs/
│   ├── plans/             # Планы реализации по этапам
│   └── specs/             # Спецификации и дизайн
├── admin/
│   └── menu.php           # Пункт меню в Bitrix-админке
├── composer.json          # Зависимости (phpunit/phpunit ^11)
└── phpunit.xml            # Конфигурация тестов
```

---

## Запуск тестов

```bash
vendor/bin/phpunit --testdox
```

Через PhpStorm: **Run → Edit Configurations → PHPUnit Local** → выбрать `phpunit.xml`.

Текущий результат: **48 тестов, 123 assertions, 0 failures**.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README.md"
```
